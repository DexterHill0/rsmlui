[package]
name = "custom-backend-advanced"
version = "0.1.0"
edition = "2024"

[dependencies]
rsmlui-core = { path = "../../rsmlui-core", features = [
    # "freetype",
    # "renderer-gl2",
    # "window-winit",
] }
# windows = "0.62.2"
