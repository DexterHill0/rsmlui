[package]
name = "rsmlui-sys"
version = "0.1.0"
edition = "2024"

[build-dependencies]
cmake = "0.1"
bindgen = "0.72.0"
cxx-build = "1.0"
cc = "1.0"
glob = "0.3.3"

[dependencies]
cxx = "1.0"

[features]
default = ["freetype"]

cosmic-text = []
freetype = []

backend-win32-gl2 = []
